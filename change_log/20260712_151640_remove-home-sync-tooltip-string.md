# Change log — Remove the unused homeSyncTooltip localization string

Implements plan
[plans/20260712_151518_remove-home-sync-tooltip-string.md](../plans/20260712_151518_remove-home-sync-tooltip-string.md).

## What changed

Removed the now-unused `homeSyncTooltip` ("Sync with another device")
localization string, left over after the sync icon was removed from the Recent
files toolbar. No stale code remains.

### Files changed

- `lib/l10n/app_en.arb` — deleted the `homeSyncTooltip` value line and its
  `@homeSyncTooltip` description line (hand-edited).
- `lib/l10n/app_localizations.dart` — regenerated by `flutter gen-l10n`
  (abstract getter removed).
- `lib/l10n/app_localizations_en.dart` — regenerated by `flutter gen-l10n`
  (English getter removed).

## Testing

- `flutter analyze lib/l10n lib/shell/home` — no issues.
- `flutter test test/l10n/localization_test.dart` — all tests pass.
- Confirmed no `.dart` file under `lib/` references `homeSyncTooltip`.
